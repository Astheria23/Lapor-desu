# Environment example for Lapor Desu (copy to .env and fill values)
# Database (Neon Postgres)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require"

# JWT secret
SECRET_KEY="change_me_strong_secret"

# Optional: Supabase Storage for image uploads
# If omitted, image upload will be disabled and creating reports with files will error
SUPABASE_URL="https://your-project-id.supabase.co"
SUPABASE_KEY="your_service_key"
BUCKET_NAME="lapor-image"
